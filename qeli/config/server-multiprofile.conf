# ============================================================================
#  qeli server — READY-MADE MULTI-PROFILE example: one profile per wire mode (10),
#  named by mode, the way a real production box is laid out (reality-tls on :443,
#  the rest on 8443-8451). Copy a profile, tweak it, delete the modes you don't
#  want. Users are global ([user:*] / users.conf) and work on every profile.
#
#  This is a TEMPLATE, not the exhaustive key reference — for every option with
#  its default see server.conf(.example). Getting started: GETTING-STARTED.md.
#
#  ⚠️ Before using: replace every CHANGEME (obfs pre-shared keys), set your own
#  reality short_ids, and add users with `qeli add-client`. For full-tunnel egress
#  (routing.nat.enabled = true below) the `iptables` package must be installed.
#
#  SNI semantics (important): obf.tls.server_name is COSMETIC for fake-tls (the
#  server ignores it; it only seeds the SNI baked into a share link). The client
#  picks its own SNI: a configured/link `sni` wins, else a random decoy from the
#  built-in pool when connecting to a bare IP, else the connect hostname. So for
#  per-connection SNI rotation on fake-tls, leave the client `sni` EMPTY and connect
#  by IP. For reality / reality-tls the SNI is NOT cosmetic — it must equal
#  reality_proxy.target (see the note on the reality-tls profile below).
#
#  Stealth posture: every masking profile here uses flow-shaping (Poisson idle
#  cover) instead of a fixed heartbeat, so there is no ~15s beacon to fingerprint.
# ============================================================================

[auth]
# reality-tls needs the client to pin the server key; require it globally so every
# share link carries key= (anti-MITM for all modes). Pairs with bind_static (H-1).
require_client_key_proof = true
bind_static_to_session = true
brute_force.max_attempts = 5
brute_force.window_secs = 60
brute_force.lockout_secs = 300

[logging]
level = info
file = /var/log/qeli/server.log

[web]
# Admin UI. Disabled here — enable it and set a password with `qeli set-web-password`
# (see PANEL.md). FAIL-CLOSED: an empty password_hash refuses to start the panel on
# ANY bind, loopback included. On a PUBLIC bind you should also set tls = true — that
# one is a loud warning rather than a refusal, but without it the admin password and
# session travel in cleartext. enabled/bind/port/tls*/base_path apply only on a full
# process restart — the panel's "Apply & Restart" button performs one (save +
# `systemctl restart`); from a shell it is `systemctl restart qeli`.
enabled = false
bind = 127.0.0.1
port = 8080
username = admin
# tls = true      # panel then speaks HTTPS — open https://<bind>:<port>, not http://
# set via `qeli set-web-password`
# password_hash = $argon2id$...
# default host for share links/QR
# public_host = vpn.example.com
# Source-IP allowlist (include your own IP!). Empty — or the equivalent "" — means ANY
# source. A blocked source gets a bare 403 on every route, so a 403 when just opening
# the panel is this filter (CSRF exempts GET and cannot block a page load). Repeated
# allowed_ips lines are FOLDED together, so a stray duplicate keeps the filter active.
# allowed_ips = 203.0.113.4

# ── 1) reality-tls — genuine TLS 1.3 carrying the tunnel — TCP :443 ──────────
[profile:reality-tls]
enabled = true
bind.address = 0.0.0.0
bind.port = 443
bind.transport = tcp
tun.name = vpn0
tun.address = 10.9.0.1
# 1280 is conservative for TCP-carried modes (nested TCP); 1400 is the default.
tun.mtu = 1400
tun.queues = 0
pool.cidr = 10.9.0.0/24
# Full-tunnel egress (programs iptables: ip_forward + MASQUERADE + MSS). Requires
# the `iptables` package. Set false for split-tunnel.
routing.nat.enabled = true
routing.forward_private = true
dns.enabled = true
dns.listen = 10.9.0.1
dns.upstream = 1.1.1.1
obf.mode = reality-tls
obf.tls.server_name = www.microsoft.com
obf.tls.reality_proxy.enabled = true
obf.tls.reality_proxy.target = www.microsoft.com
obf.tls.reality_proxy.target_port = 443
obf.tls.reality_proxy.real_tls = true
# Example short_id — GENERATE YOUR OWN (qeli / gen_reality_link); clients must send one.
obf.tls.reality_proxy.short_ids = 0123456789abcdef
# reality-tls runs inside real TLS, so on-wire padding adds only overhead — off.
obf.padding.enabled = false
# SNI is NOT cosmetic here: reality-tls mimics ONE real site (reality_proxy.target),
# so the client MUST send server_name == target or the disguise breaks (SNI would not
# match the relayed cert). Do not rotate it. For several front domains, run several
# reality-tls profiles, each with its own target + matching client links.
obf.heartbeat.enabled = false
# Idle cover (parity with the prod maxobf posture): hide the dead-air / beacon tell
# even inside the real-TLS session. Zero added latency; costs only idle bandwidth.
obf.traffic_shaping.enabled = true
obf.traffic_shaping.idle_gap_mean_ms = 700
obf.traffic_shaping.idle_gap_min_ms = 40
obf.traffic_shaping.idle_gap_max_ms = 6000
obf.traffic_shaping.budget_bytes_per_sec = 16384
obf.quic.enabled = false
# Stream bonding (multipath): N parallel connections aggregated into one session
# (TCP modes only). adaptive = ramp up under load.
obf.multipath.enabled = true
obf.multipath.adaptive = true
perf.tcp.nodelay = true
perf.connection.max_clients = 64
perf.connection.handshake_timeout_secs = 10
perf.connection.idle_timeout_secs = 0

# ── 2) reality — REALITY proxy (fake-TLS peek-and-decide, no inner TLS) — TCP :8443
[profile:reality]
enabled = true
bind.address = 0.0.0.0
bind.port = 8443
bind.transport = tcp
tun.name = vpn1
tun.address = 10.9.1.1
tun.mtu = 1400
tun.queues = 0
pool.cidr = 10.9.1.0/24
routing.nat.enabled = true
routing.forward_private = true
dns.enabled = true
dns.listen = 10.9.1.1
dns.upstream = 1.1.1.1
obf.mode = fake-tls
obf.tls.server_name = www.microsoft.com
obf.tls.reality_proxy.enabled = true
obf.tls.reality_proxy.target = www.microsoft.com
obf.tls.reality_proxy.target_port = 443
obf.tls.reality_proxy.real_tls = false
obf.tls.reality_proxy.short_ids = fedcba9876543210
obf.padding.enabled = true
obf.padding.min_bytes = 40
obf.padding.max_bytes = 400
# Flow-shaping: Poisson-gapped idle cover REPLACES the fixed heartbeat, killing the
# ~15s small-packet beacon (a timing fingerprint). Empty encrypted records the peer
# drops; real packets are never delayed (zero added latency), only genuine idle is
# filled, capped by the budget below. Works on TCP and UDP profiles alike.
obf.heartbeat.enabled = false
obf.traffic_shaping.enabled = true
obf.traffic_shaping.idle_gap_mean_ms = 700
obf.traffic_shaping.idle_gap_min_ms = 40
obf.traffic_shaping.idle_gap_max_ms = 6000
obf.traffic_shaping.budget_bytes_per_sec = 16384
obf.traffic_shaping.min_size = 64
obf.traffic_shaping.max_size = 1024
obf.quic.enabled = false
obf.multipath.enabled = true
obf.multipath.adaptive = true
perf.tcp.nodelay = true
perf.connection.max_clients = 64
perf.connection.handshake_timeout_secs = 10
perf.connection.idle_timeout_secs = 0

# ── 3) fake-tls — TLS-1.3-mimicking handshake — TCP :8444 ────────────────────
[profile:fake-tls]
enabled = true
bind.address = 0.0.0.0
bind.port = 8444
bind.transport = tcp
tun.name = vpn2
tun.address = 10.9.2.1
tun.mtu = 1400
tun.queues = 0
pool.cidr = 10.9.2.0/24
routing.nat.enabled = true
routing.forward_private = true
dns.enabled = true
dns.listen = 10.9.2.1
dns.upstream = 1.1.1.1
obf.mode = fake-tls
obf.tls.server_name = www.microsoft.com
obf.padding.enabled = true
obf.padding.min_bytes = 40
obf.padding.max_bytes = 400
# Flow-shaping: Poisson-gapped idle cover REPLACES the fixed heartbeat, killing the
# ~15s small-packet beacon (a timing fingerprint). Empty encrypted records the peer
# drops; real packets are never delayed (zero added latency), only genuine idle is
# filled, capped by the budget below. Works on TCP and UDP profiles alike.
obf.heartbeat.enabled = false
obf.traffic_shaping.enabled = true
obf.traffic_shaping.idle_gap_mean_ms = 700
obf.traffic_shaping.idle_gap_min_ms = 40
obf.traffic_shaping.idle_gap_max_ms = 6000
obf.traffic_shaping.budget_bytes_per_sec = 16384
obf.traffic_shaping.min_size = 64
obf.traffic_shaping.max_size = 1024
obf.quic.enabled = false
obf.multipath.enabled = true
obf.multipath.adaptive = true
perf.tcp.nodelay = true
perf.connection.max_clients = 64
perf.connection.handshake_timeout_secs = 10
perf.connection.idle_timeout_secs = 0

# ── 4) obfs-ws — ChaCha20 stream obfs + WebSocket-fronting — TCP :8445 ───────
[profile:obfs-ws]
enabled = true
bind.address = 0.0.0.0
bind.port = 8445
bind.transport = tcp
tun.name = vpn3
tun.address = 10.9.3.1
tun.mtu = 1400
tun.queues = 0
pool.cidr = 10.9.3.0/24
routing.nat.enabled = true
routing.forward_private = true
dns.enabled = true
dns.listen = 10.9.3.1
dns.upstream = 1.1.1.1
obf.mode = obfs
# REQUIRED for obfs: a strong shared secret that MUST match the client.
obf.obfs_key = CHANGEME-obfs-ws-psk
# obf.obfs_fronting = websocket now emits REAL WebSocket binary frames (not just an
# HTTP-looking first-byte preamble), so the stream reads as a genuine WS upgrade.
obf.obfs_fronting = websocket
# AmneziaWG-style junk: before the handshake, prepend jc junk packets of jmin..jmax random
# bytes to defeat pre-handshake signature/FET matching. Works on TCP `obfs` AND every UDP
# mode (fake-tls/quic/obfs) — it has NO effect only on a TCP fake-tls/reality-tls profile,
# where junk before the real TLS ClientHello would break the mimicry (the server warns + the
# share link omits awg there). On TCP the CLIENT MUST set the SAME jc (awg=true, jc=4,
# jmin=40, jmax=200) — both ends agree on the count; on UDP jc is sender-only (see udp-obfs).
obf.awg.enabled = true
obf.awg.jc = 4
obf.awg.jmin = 40
obf.awg.jmax = 200
obf.padding.enabled = false
# Flow-shaping: Poisson-gapped idle cover REPLACES the fixed heartbeat, killing the
# ~15s small-packet beacon (a timing fingerprint). Empty encrypted records the peer
# drops; real packets are never delayed (zero added latency), only genuine idle is
# filled, capped by the budget below. Works on TCP and UDP profiles alike.
obf.heartbeat.enabled = false
obf.traffic_shaping.enabled = true
obf.traffic_shaping.idle_gap_mean_ms = 700
obf.traffic_shaping.idle_gap_min_ms = 40
obf.traffic_shaping.idle_gap_max_ms = 6000
obf.traffic_shaping.budget_bytes_per_sec = 16384
obf.traffic_shaping.min_size = 64
obf.traffic_shaping.max_size = 1024
obf.quic.enabled = false
obf.multipath.enabled = true
obf.multipath.adaptive = true
perf.tcp.nodelay = true
perf.connection.max_clients = 64
perf.connection.handshake_timeout_secs = 10
perf.connection.idle_timeout_secs = 0

# ── 5) obfs-none — ChaCha20 stream obfs, no fronting — TCP :8446 ─────────────
[profile:obfs-none]
enabled = true
bind.address = 0.0.0.0
bind.port = 8446
bind.transport = tcp
tun.name = vpn4
tun.address = 10.9.4.1
tun.mtu = 1400
tun.queues = 0
pool.cidr = 10.9.4.0/24
routing.nat.enabled = true
routing.forward_private = true
dns.enabled = true
dns.listen = 10.9.4.1
dns.upstream = 1.1.1.1
obf.mode = obfs
obf.obfs_key = CHANGEME-obfs-none-psk
obf.obfs_fronting = none
# AmneziaWG-style pre-handshake junk, OFF here. This is a TCP obfs profile, so enable on
# BOTH ends with the SAME jc (the receiver skips exactly jc records; on UDP profiles jc is
# sender-only instead). jc = junk packet count (0 = off, max 128), jmin/jmax = random size
# (jmin <= jmax <= 1400). Defaults: enabled=false, jc=0, jmin=40, jmax=300.
# obf.awg.enabled = true
# obf.awg.jc = 4
# obf.awg.jmin = 40
# obf.awg.jmax = 300
obf.padding.enabled = true
obf.padding.min_bytes = 40
obf.padding.max_bytes = 400
# Flow-shaping: Poisson-gapped idle cover REPLACES the fixed heartbeat, killing the
# ~15s small-packet beacon (a timing fingerprint). Empty encrypted records the peer
# drops; real packets are never delayed (zero added latency), only genuine idle is
# filled, capped by the budget below. Works on TCP and UDP profiles alike.
obf.heartbeat.enabled = false
obf.traffic_shaping.enabled = true
obf.traffic_shaping.idle_gap_mean_ms = 700
obf.traffic_shaping.idle_gap_min_ms = 40
obf.traffic_shaping.idle_gap_max_ms = 6000
obf.traffic_shaping.budget_bytes_per_sec = 16384
obf.traffic_shaping.min_size = 64
obf.traffic_shaping.max_size = 1024
obf.quic.enabled = false
obf.multipath.enabled = true
obf.multipath.adaptive = true
perf.tcp.nodelay = true
perf.connection.max_clients = 64
perf.connection.handshake_timeout_secs = 10
perf.connection.idle_timeout_secs = 0

# ── 6) plain — raw tunnel, no obfuscation (TCP-only by design) — TCP :8447 ───
[profile:plain]
enabled = true
bind.address = 0.0.0.0
bind.port = 8447
bind.transport = tcp
tun.name = vpn5
tun.address = 10.9.5.1
tun.mtu = 1400
tun.queues = 0
pool.cidr = 10.9.5.0/24
routing.nat.enabled = true
routing.forward_private = true
dns.enabled = true
dns.listen = 10.9.5.1
dns.upstream = 1.1.1.1
obf.mode = plain
obf.heartbeat.enabled = true
obf.heartbeat.interval_ms = 15000
obf.multipath.enabled = true
obf.multipath.adaptive = true
perf.tcp.nodelay = true
perf.connection.max_clients = 64
perf.connection.handshake_timeout_secs = 10
perf.connection.idle_timeout_secs = 0

# ── 7) udp-fake-tls — fake-TLS over UDP — UDP :8448 ─────────────────────────
[profile:udp-fake-tls]
enabled = true
bind.address = 0.0.0.0
bind.port = 8448
bind.transport = udp
tun.name = vpn6
tun.address = 10.9.6.1
tun.mtu = 1400
tun.queues = 0
pool.cidr = 10.9.6.0/24
routing.nat.enabled = true
routing.forward_private = true
dns.enabled = true
dns.listen = 10.9.6.1
dns.upstream = 1.1.1.1
obf.mode = fake-tls
obf.tls.server_name = www.microsoft.com
obf.padding.enabled = true
obf.padding.min_bytes = 40
obf.padding.max_bytes = 400
# Flow-shaping: Poisson-gapped idle cover REPLACES the fixed heartbeat, killing the
# ~15s small-packet beacon (a timing fingerprint). Empty encrypted records the peer
# drops; real packets are never delayed (zero added latency), only genuine idle is
# filled, capped by the budget below. Works on TCP and UDP profiles alike.
obf.heartbeat.enabled = false
obf.traffic_shaping.enabled = true
obf.traffic_shaping.idle_gap_mean_ms = 700
obf.traffic_shaping.idle_gap_min_ms = 40
obf.traffic_shaping.idle_gap_max_ms = 6000
obf.traffic_shaping.budget_bytes_per_sec = 16384
obf.traffic_shaping.min_size = 64
obf.traffic_shaping.max_size = 1024
obf.quic.enabled = false
perf.connection.max_clients = 64
perf.connection.handshake_timeout_secs = 10
perf.connection.idle_timeout_secs = 0

# ── 8) udp-quic — fake-TLS + QUIC masking over UDP — UDP :8449 ──────────────
[profile:udp-quic]
enabled = true
bind.address = 0.0.0.0
bind.port = 8449
bind.transport = udp
tun.name = vpn7
tun.address = 10.9.7.1
tun.mtu = 1400
tun.queues = 0
pool.cidr = 10.9.7.0/24
routing.nat.enabled = true
routing.forward_private = true
dns.enabled = true
dns.listen = 10.9.7.1
dns.upstream = 1.1.1.1
obf.mode = fake-tls
obf.tls.server_name = www.microsoft.com
obf.quic.enabled = true
obf.padding.enabled = true
obf.padding.min_bytes = 40
obf.padding.max_bytes = 400
# Flow-shaping: Poisson-gapped idle cover REPLACES the fixed heartbeat, killing the
# ~15s small-packet beacon (a timing fingerprint). Empty encrypted records the peer
# drops; real packets are never delayed (zero added latency), only genuine idle is
# filled, capped by the budget below. Works on TCP and UDP profiles alike.
obf.heartbeat.enabled = false
obf.traffic_shaping.enabled = true
obf.traffic_shaping.idle_gap_mean_ms = 700
obf.traffic_shaping.idle_gap_min_ms = 40
obf.traffic_shaping.idle_gap_max_ms = 6000
obf.traffic_shaping.budget_bytes_per_sec = 16384
obf.traffic_shaping.min_size = 64
obf.traffic_shaping.max_size = 1024
perf.connection.max_clients = 64
perf.connection.handshake_timeout_secs = 10
perf.connection.idle_timeout_secs = 0

# ── 9) udp-obfs — ChaCha20 stream obfs over UDP — UDP :8450 ─────────────────
[profile:udp-obfs]
enabled = true
bind.address = 0.0.0.0
bind.port = 8450
bind.transport = udp
tun.name = vpn8
tun.address = 10.9.8.1
tun.mtu = 1400
tun.queues = 0
pool.cidr = 10.9.8.0/24
routing.nat.enabled = true
routing.forward_private = true
dns.enabled = true
dns.listen = 10.9.8.1
dns.upstream = 1.1.1.1
obf.mode = obfs
obf.obfs_key = CHANGEME-udp-obfs-psk
# AmneziaWG-style pre-handshake junk — works on UDP too, OFF here. On UDP `jc` is
# SENDER-ONLY: the client prepends `jc` decoy datagrams before its ClientHello and the
# server drops them cheaply (before its rate limiter), so nothing needs to match and a
# lost/reordered junk datagram is harmless. jc = decoy count (0 = off, max 128); jmin/jmax
# = random size (jmin <= jmax <= 1400; each capped at 1200 on UDP so it never IP-fragments).
# obf.awg.enabled = true
# obf.awg.jc = 4
# obf.awg.jmin = 40
# obf.awg.jmax = 300
obf.padding.enabled = false
# Flow-shaping: Poisson-gapped idle cover REPLACES the fixed heartbeat, killing the
# ~15s small-packet beacon (a timing fingerprint). Empty encrypted records the peer
# drops; real packets are never delayed (zero added latency), only genuine idle is
# filled, capped by the budget below. Works on TCP and UDP profiles alike.
obf.heartbeat.enabled = false
obf.traffic_shaping.enabled = true
obf.traffic_shaping.idle_gap_mean_ms = 700
obf.traffic_shaping.idle_gap_min_ms = 40
obf.traffic_shaping.idle_gap_max_ms = 6000
obf.traffic_shaping.budget_bytes_per_sec = 16384
obf.traffic_shaping.min_size = 64
obf.traffic_shaping.max_size = 1024
perf.connection.max_clients = 64
perf.connection.handshake_timeout_secs = 10
perf.connection.idle_timeout_secs = 0

# ── 10) obfs-awg — obfs + AmneziaWG-style junk preamble (the Amnezia analog) — TCP :8451 ─
# A raw ChaCha20 obfs stream preceded by `jc` random junk packets before the nonce
# exchange (mimics AmneziaWG's Jc/Jmin/Jmax). "obfs mode + obf.awg.enabled" IS the
# whole mode — there is no separate wire mode. The CLIENT must set the SAME jc (it
# rides in the qeli:// link / QR automatically).
[profile:obfs-awg]
enabled = true
bind.address = 0.0.0.0
bind.port = 8451
bind.transport = tcp
tun.name = vpn9
tun.address = 10.9.9.1
tun.mtu = 1400
tun.queues = 0
pool.cidr = 10.9.9.0/24
routing.nat.enabled = true
routing.forward_private = true
dns.enabled = true
dns.listen = 10.9.9.1
dns.upstream = 1.1.1.1
obf.mode = obfs
obf.obfs_key = CHANGEME-obfs-awg-psk
obf.obfs_fronting = none
# The defining feature of this profile — both ends MUST use the same jc (0 = off,
# max 128); jmin/jmax = random junk size bounds (jmin <= jmax <= 1400).
obf.awg.enabled = true
obf.awg.jc = 4
obf.awg.jmin = 40
obf.awg.jmax = 200
obf.padding.enabled = true
obf.padding.min_bytes = 40
obf.padding.max_bytes = 400
obf.heartbeat.enabled = false
obf.traffic_shaping.enabled = true
obf.traffic_shaping.idle_gap_mean_ms = 700
obf.traffic_shaping.idle_gap_min_ms = 40
obf.traffic_shaping.idle_gap_max_ms = 6000
obf.traffic_shaping.budget_bytes_per_sec = 16384
obf.traffic_shaping.min_size = 64
obf.traffic_shaping.max_size = 1024
obf.quic.enabled = false
obf.multipath.enabled = true
obf.multipath.adaptive = true
perf.tcp.nodelay = true
perf.connection.max_clients = 64
perf.connection.handshake_timeout_secs = 10
perf.connection.idle_timeout_secs = 0

# ── users (global — usable on every profile) ────────────────────────────────
# Users live in a separate file — auth.users_file (default /etc/qeli/users.conf).
# Add with:  qeli add-client <name> --password <pass> --config <this file>
# (Argon2id-hashes the password and appends a [user:<name>] section to that FILE).
# You may instead inline [user:*] sections here, but then auth.users_file is
# ignored (inline wins) — don't set both.
