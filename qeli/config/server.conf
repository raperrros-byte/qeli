# ============================================================================
#  qeli server — flat-INI config (the single supported format; TOML/JSON dropped)
# ----------------------------------------------------------------------------
#  This file is an EXHAUSTIVE reference: every key the server understands is
#  listed with its DEFAULT value and a one-line note. A real deployment only
#  needs a handful of keys (any omitted key falls back to the default shown
#  here), so feel free to delete what you don't change.
#
#  NOTE: comments must be on their OWN line (a leading '#'). Inline comments
#  after a value are NOT stripped — they become part of the value.
#
#  Structure:
#    [auth] [web] [logging]       — global singleton sections
#    [profile:<name>]             — one per interface; nested fields are dotted
#                                   keys (bind.port, obf.mode, perf.connection.*)
#    [user:<name>] [group:<name>] — inline users/groups (or auth.users_file)
#
#  Pin the server key on clients:  qeli show-identity --config /etc/qeli/server.conf
# ============================================================================

[auth]
# Path to the standalone user database (used when no inline [user:*] sections).
users_file = /etc/qeli/users.conf
# true = reject clients that did not pin the server key (no TOFU). Default false.
require_client_key_proof = false
# H-1 — bind the data-plane session keys to this server's static identity (Noise-IK):
# the KDF also folds in es = X25519(client_eph, server_static), so a failed ephemeral
# RNG alone no longer exposes the tunnel. **Default true (since 0.7.1).** WIRE-BREAKING:
# only clients that pin this server's key AND run H-1 (their default) can connect — a
# legacy 0.7.0 / TOFU client is rejected. Implies pinning, so it pairs with
# require_client_key_proof = true. Set false ONLY to interoperate with a 0.7.0 fleet
# during a staged upgrade.
bind_static_to_session = true
# Brute-force lockout for VPN AUTHENTICATION (per source IP). A username under
# attack gets an adaptive tarpit instead of a hard lock, so a correct password
# always passes. This policy is INDEPENDENT of the web-panel login policy — that
# one lives in [web] brute_force below. Set enabled = false to turn VPN-auth
# rate-limiting off entirely (e.g. behind an external limiter).
brute_force.enabled = true
brute_force.max_attempts = 5
brute_force.window_secs = 300
brute_force.lockout_secs = 900

[web]
# Admin UI. Disabled by default. Full guide: docs/{ru,eng}/PANEL.md
enabled = false
# Bind 127.0.0.1 = reach it over an SSH tunnel (ssh -L 8080:127.0.0.1:8080 root@host).
# A public IP needs tls + password_hash below (see fail-closed note).
# RESTART SCOPE: enabled / bind / port / tls / tls_cert / tls_key / base_path are bound
# when the PROCESS starts, so changing them needs a FULL restart. The panel's
# "Apply & Restart" button does exactly that (save, then `systemctl restart`), and your
# panel login survives it while persist_session_key is on (the default). From a shell it
# is `systemctl restart qeli`. Everything else in [web] (password, allowed_ips,
# allowed_origins, csrf, public_host, …) reloads live, with no restart at all.
# With tls = true the panel speaks HTTPS — open https://<bind>:<port>, not http://.
bind = 127.0.0.1
port = 8080
username = admin
# Argon2id hash of the panel password. EASIEST: let qeli set it for you (works on a
# fresh install with no panel access yet) — generates+enables+writes it here:
#   qeli set-web-password                       # random password, printed once
#   qeli set-web-password --password 'PASS'     # your own password
# Or set it in-panel (Config -> Web -> Set admin password), or hand-roll a hash:
#   printf '%s' 'PASS' | argon2 "$(head -c12 /dev/urandom|base64)" -id -t 3 -m 15 -p 1 -e
# FAIL-CLOSED: an empty password_hash refuses to start the panel on ANY bind —
# loopback included (an open panel on 127.0.0.1 is still full admin for every local
# process, and for anything on the host that can be induced to issue a request: SSRF).
# Only web.insecure_no_auth = true deliberately serves it without a password.
# password_hash = $argon2id$...
# Native HTTPS (rustls) so the panel can be exposed on a public IP without a proxy.
# Empty tls_cert/tls_key = self-signed, auto-generated & persisted to
# /etc/qeli/web-tls-*.pem (browser warns once). Set both for a real (Let's Encrypt) cert.
# tls = true
# tls_cert = /etc/letsencrypt/live/vpn.example.com/fullchain.pem
# tls_key  = /etc/letsencrypt/live/vpn.example.com/privkey.pem
# Source-IP allowlist (CIDRs / bare IPs). Strongest barrier for a public bind —
# include your own admin IP or you'll 403 yourself.
#   allowed_ips =            no filter: ANY source may reach the panel
#   allowed_ips = ""         identical to the line above (surrounding quotes are
#                            stripped, so this is just an explicit way to write "empty")
#   allowed_ips = 203.0.113.4, 10.0.0.0/8      only these sources may reach it
# A blocked source gets a BARE 403 on EVERY route (pages, assets, API). So a 403 when
# merely OPENING the panel is always this allowlist — it is never CSRF, which exempts
# GET/HEAD/OPTIONS and so cannot block a page load.
# GOTCHA: repeated `allowed_ips` lines are FOLDED into a single list (every occurrence
# in this section counts, not just the last one). A leftover duplicate elsewhere in
# [web] therefore keeps the allowlist active even when the line you are looking at is
# empty — `grep -n allowed_ips` the whole file when a 403 makes no sense.
# allowed_ips = 203.0.113.4, 10.0.0.0/8
# Default public host for share links/QR (pre-fills the Share dialog; editable there).
# public_host = vpn.example.com
# Extra browser origins (host[:port]) accepted by the CSRF check when the panel is
# reached via a domain / reverse proxy. Without it a public panel loads but every
# save returns 403. The bind host + public_host are already trusted implicitly.
# allowed_origins = panel.example.com, vpn.example.com:8443
# Add the `Secure` flag to the session cookie. Auto-on when tls = true; set it
# manually only behind a TLS reverse proxy. Never on plain HTTP (would lock you out).
secure_cookie = false
# Trusted reverse-proxy CIDRs/IPs whose X-Forwarded-For is honoured for the
# source-IP allowlist + rate-limit. Only real upstreams — a spoofable entry lets a
# client forge its source IP. Empty (default) = trust the direct peer only.
# trusted_proxies = 10.0.0.0/8, 203.0.113.4
# Mount the panel under a sub-path when it sits behind a path-routing reverse proxy.
# Empty (default) = served at the root.
# base_path = /qeli
# CSRF same-origin protection (default true). Turn OFF only for a trusted private
# setup where an upstream strips the Origin header — otherwise any site could drive it.
# Applies to STATE-CHANGING requests only (GET/HEAD/OPTIONS are exempt), so it can
# never 403 a page load — a 403 on opening the panel is the allowed_ips filter above.
# Its 403 also carries an explanatory text body naming the rejected Origin.
# csrf = true
# Admin session lifetime in seconds (default 86400 = 1 day; capped at 30 days).
# session_ttl_secs = 86400
# Let the panel check GitHub Releases for a newer qeli and show a notice (opt-in,
# notify-only — never downloads or installs). Default false.
# update_check = false
# Brute-force lockout for PANEL LOGIN (per source IP) — independent of the VPN-auth
# policy in [auth] brute_force. Own attempt count, window and lockout so the panel
# and the tunnel can be tuned separately. Defaults shown; set enabled = false to
# turn panel-login rate-limiting off (only safe on a trusted / loopback bind).
brute_force.enabled = true
brute_force.max_attempts = 5
brute_force.window_secs = 300
brute_force.lockout_secs = 900

[logging]
# error | warn | info | debug
level = info
# Log file; omit -> stderr / journald.
# file = /var/log/qeli/server.log
# Shape of the timestamp prefix on every line:
#   datetime  2026-07-18 18:10:03.259    local time (default)
#   rfc3339   2026-07-18T18:10:03.259Z   UTC — correlate across hosts, ship to Loki/ELK
#   time      18:10:03.259               local, no date — narrow/embedded logs
#   epoch     1782000603.259             unix seconds.millis (UTC)
#   none      (no prefix)                journald/syslog already stamps the line
# An unknown value falls back to `datetime`.
time_format = datetime
# NOTE: `format` (the LINE format: plain/json/logfmt/…) is parsed and shown in the
# panel, but init_logging never reads it yet — the line is always
# `<timestamp> LEVEL target: message`. Tracked in ROADMAP; don't count on JSON logs.

# ── A TCP profile: fake-TLS on :443. One [profile:<name>] per interface. ─────
[profile:tcp]
# true (default) = bound and served; false = kept but skipped at startup (turn a
# profile off without deleting it). At least one profile must be enabled.
enabled = true
# This profile's static identity (X25519) private key. Default: per-profile
# /etc/qeli/identity/<name>.key (auto-created on first start).
# identity_key = /etc/qeli/identity/tcp.key

# -- bind --------------------------------------------------------------------
bind.address = 0.0.0.0
bind.port = 443
# tcp | udp  (udp requires obf.mode = fake-tls; plain is TCP-only)
bind.transport = tcp

# -- tun ---------------------------------------------------------------------
# The tunnel subnet must not overlap ANY network this host already uses — its own
# addresses, its default gateway, or a LAN it routes. Overlapping the gateway is the
# worst case: the TUN takes the gateway's address, every outbound packet dies in the
# tunnel, and the server drops off the network entirely (SSH and ping included), with
# nothing in the log to say why. 10.9.x is used here because 10.0.0.0/24 — the previous
# default — is one of the most common VPS gateway subnets. The server now refuses to
# start on such a collision (see `qeli check-config`), but pick a free range anyway.
tun.name = vpn0
tun.address = 10.9.0.1
# TUN MTU, also PUSHED to clients at auth (a client on mtu=0/auto adopts it).
tun.mtu = 1400
# tx queue length of the TUN device.
tun.tx_queue_len = 1000
# tun | tap
tun.device_type = tun
# Multi-queue TUN (data-plane parallelism). 0 = auto (= CPU count); N = N queues;
# 1 = legacy single pump. Linux IFF_MULTI_QUEUE; server-only, no wire change.
tun.queues = 0

# -- address pool ------------------------------------------------------------
pool.cidr = 10.9.0.0/24
# Additional IPs never handed out (comma-separated). The server TUN address is
# reserved automatically and must not be duplicated here.
# Static IP reservations by username (repeatable):
# pool.reservation.alice = 10.9.0.100

# -- routing -----------------------------------------------------------------
# Allow client<->client traffic within the tunnel subnet.
routing.client_to_client = false
# Forward RFC1918/private networks reachable behind the server to clients.
routing.forward_private = true
# Full-tunnel egress. When true the server itself programs the host firewall via
# `iptables`: enables net.ipv4.ip_forward, MASQUERADEs the pool out the WAN
# interface, and MSS-clamps forwarded TCP — and removes those rules again on stop.
# REQUIRES the `iptables` package (the .deb depends on it); if it is missing the
# server logs an ERROR and the web panel shows a warning. Default false (split-tunnel).
routing.nat.enabled = false
# WAN egress interface. Empty (or the legacy "eth0" placeholder) = auto-detect via
# `ip route get 1.1.1.1`; set explicitly (e.g. ens3) to override.
routing.nat.interface =
# Advertised route(s) pushed to clients — REPEATABLE, and applied by every client
# regardless of its `route_local` setting (since 0.7.12).
# Format: the CIDR comes FIRST and bare. `gateway=` is the NEXT-HOP IP (not a subnet;
# defaults to this profile's tun.address) and `metric=` defaults to 100.
# A line whose CIDR is missing or malformed is REJECTED at load with a warning — it is
# never pushed. (Leaving the CIDR empty and putting the subnet in `gateway=` produces
# `route = " gateway=… "`, which is exactly that error.)
# route = 192.168.50.0/24
# route = 10.20.0.0/16 gateway=10.9.0.1 metric=100

# -- in-tunnel DNS proxy -----------------------------------------------------
# When disabled (the default) the server pushes NO DNS and clients keep their own
# resolvers. Set true to run the in-tunnel resolver defined below.
dns.enabled = false
# Push a specific resolver to clients WITHOUT running the built-in proxy — e.g. a LAN /
# AdGuard / NextDNS box. First IP of the list is pushed in auth-OK; clients in `dns = tunnel`
# apply it. Empty = the old behaviour (listen-IP when dns.enabled, else nothing).
# dns.push_servers = 192.168.1.1
dns.listen = 10.9.0.1
dns.port = 53
# Upstream resolvers (comma-separated).
dns.upstream = 1.1.1.1, 8.8.8.8
dns.cache_size = 1000
dns.timeout_secs = 5
# Domains answered with 0.0.0.0 (ad/track blocking), comma-separated:
# dns.blocklist = ads.example.com, track.example.com

# -- DHCP server (optional; most setups push IPs at auth instead) ------------
dhcp.enabled = false
dhcp.listen = 0.0.0.0:67
# dhcp.pool_start = 10.9.0.50
# dhcp.pool_end = 10.9.0.200
dhcp.lease_time_secs = 86400
dhcp.domain_name = vpn

# -- obfuscation: wire mode --------------------------------------------------
# Wire mode: plain | fake-tls | obfs.  (reality-tls = fake-tls + reality_proxy
# below with real_tls=true.)  obfs is TCP-only; plain is TCP-only.
obf.mode = fake-tls
# Pre-shared key, REQUIRED for obf.mode = obfs (must match the client):
# obf.obfs_key = shared-secret
# obfs anti-FET fronting: websocket (default, emits REAL WebSocket binary frames) | none.
obf.obfs_fronting = websocket
# obfs anti-FET junk (AmneziaWG-style, obfs-only): before the handshake, prepend jc junk
# packets of jmin..jmax random bytes to break pre-handshake signature/FET matching. OFF
# by default; enable on BOTH ends with the SAME jc or the handshake fails.
#   obf.awg.enabled = false   # true = send junk (obfs mode only)
#   obf.awg.jc = 0            # junk packet count (0 = off, max 128)
#   obf.awg.jmin = 40         # min junk bytes per packet
#   obf.awg.jmax = 300        # max junk bytes per packet (jmin <= jmax <= 1400)

# -- obfuscation: fake-TLS handshake shaping ---------------------------------
obf.tls.server_name = www.cloudflare.com
# NOTE: obf.tls.session_id / supported_groups / key_share_entropy_bytes used to be
# listed here. Nothing ever read them — they were pure decoration (found by
# `qeli check-config`). The REALITY token in the ClientHello session_id is driven by
# obf.tls.reality_proxy.short_ids below, and the ClientHello fingerprint is not tunable:
# in reality-tls it is a byte-exact Chrome hello on purpose, so a knob that shifted the
# named groups would only make the fingerprint unique again.

# -- obfuscation: REALITY proxy (anti active-probing) ------------------------
# When enabled, "foreign" handshakes (probes) are transparently bridged to a real
# site; only authenticated qeli clients get the tunnel.
obf.tls.reality_proxy.enabled = false
# Real site probes are bridged to (and whose cert/JA3S we borrow in real_tls mode).
obf.tls.reality_proxy.target = www.cloudflare.com
obf.tls.reality_proxy.target_port = 443
# REALITY short_ids accepted from clients (hex, <=8 bytes), comma-separated. Empty
# = legacy ALPN-absence detection. Generate with `qeli` / `gen_reality_link`.
# obf.tls.reality_proxy.short_ids = 7e78a17ad41f1004
# true = terminate authenticated clients inside a GENUINE TLS 1.3 session (real TLS
# on the wire, "reality-tls"); false (default) = fake-TLS directly on the socket.
obf.tls.reality_proxy.real_tls = false
# With real_tls: true (default) = hand-rolled byte-grade TLS that BORROWS the
# target's real cert chain + mirrors its ServerHello JA3S (Xray-REALITY parity);
# false = rustls (self-signed, weaker camouflage). Both need realtls-capable clients.
obf.tls.reality_proxy.handrolled = true
# How long (ms) to peek the ClientHello before classifying peer as client vs probe.
# Higher tolerates slow/high-latency clients without mis-bridging them to the decoy.
obf.tls.reality_proxy.peek_timeout_ms = 1500

# -- obfuscation: padding ----------------------------------------------------
obf.padding.enabled = true
obf.padding.min_bytes = 32
obf.padding.max_bytes = 512
obf.padding.randomize = true
# Fraction of packets padded (0.0-1.0).
obf.padding.probability = 1.0

# -- obfuscation: fragmentation ----------------------------------------------
obf.fragmentation.enabled = true
# Chunk sizes for the split (bytes) and the cap on how many pieces one record is
# cut into; randomised within the range on every handshake, so the split pattern
# itself isn't a signature. The goal is that the ServerHello not arrive in ONE
# segment (where a matcher reads it whole) — not that it be shredded: many tiny
# segments defeat the matcher but are themselves an anomaly no real TLS server
# produces. These defaults give 2-4 plausibly sized segments.
obf.fragmentation.min_chunk_size = 256
obf.fragmentation.max_chunk_size = 1024
obf.fragmentation.max_fragments_per_packet = 4

# -- obfuscation: heartbeat (cover traffic) ----------------------------------
obf.heartbeat.enabled = true
obf.heartbeat.interval_ms = 15000
obf.heartbeat.data_size_bytes = 16
obf.heartbeat.jitter_ms = 20

# -- obfuscation: flow shaping (idle cover; DPI-AUDIT 6.1/6.2) ----------------
# When enabled, an idle tunnel emits cover traffic at exponential (non-periodic)
# gaps instead of a fixed heartbeat (which it replaces) — kills the heartbeat
# beacon + dead-air tells. Cover = empty encrypted records the peer drops, so
# this is NOT wire-breaking. Off by default; costs only idle bandwidth.
obf.traffic_shaping.enabled = false
obf.traffic_shaping.idle_gap_mean_ms = 700
obf.traffic_shaping.idle_gap_min_ms = 40
obf.traffic_shaping.idle_gap_max_ms = 6000
obf.traffic_shaping.budget_bytes_per_sec = 16384
obf.traffic_shaping.min_size = 64
obf.traffic_shaping.max_size = 1024
# STEALTH (trades throughput for DPI passability): rate-cap the data plane to
# stealth_rate_mbps AND run cover under load, so the flow stops looking like a
# line-rate bulk download (size + timing tell). Requires enabled = true.
obf.traffic_shaping.stealth = false
obf.traffic_shaping.stealth_rate_mbps = 2

# -- obfuscation: extras -----------------------------------------------------
# Pad records up to fixed "round" sizes to flatten the size histogram.
obf.traffic_normalization.enabled = false
obf.traffic_normalization.round_sizes = 64, 128, 256, 512, 1024, 1500
# Handshake anti-fingerprinting: a constant server think-time between the
# ClientHello and the reply is itself a tell, so spread the reply over a few ms.
# Costs the client nothing; the jitter is the only knob here.
obf.anti_fingerprinting.enabled = false
obf.anti_fingerprinting.add_jitter_to_handshake = true
# UDP/QUIC masking (udp profiles only).
obf.quic.enabled = false

# -- stream bonding (multipath; TCP modes only) ------------------------------
# Aggregate several parallel connections into one tunnel to beat the
# single-stream TCP-over-TCP ceiling. Leave off / max_streams=1 on UDP profiles.
obf.multipath.enabled = false
# Hard ceiling of parallel streams per session (also the fixed count if !adaptive).
obf.multipath.max_streams = 4
# false = open exactly max_streams; true = auto-ramp by measured throughput.
obf.multipath.adaptive = false

# -- performance: TCP socket -------------------------------------------------
perf.tcp.nodelay = true
perf.tcp.keepalive_secs = 60
# Socket buffers (SO_SNDBUF / SO_RCVBUF), bytes. 0 = leave the kernel default and
# its auto-tuning alone — raise only on a high bandwidth-delay-product path, where
# the default ceiling caps a single connection. The kernel doubles what you ask for.
perf.tcp.send_buffer_size = 0
perf.tcp.recv_buffer_size = 0

# -- performance: TUN pump ---------------------------------------------------
perf.tun.read_buffer_size = 65535

# -- performance: connection limits ------------------------------------------
# Total sessions this PROFILE accepts (all users). Per-USER device caps are the
# `max_sessions` key on [user:]/[group:] below.
perf.connection.max_clients = 128
perf.connection.handshake_timeout_secs = 10
# 0 = never policy-idle-drop (heartbeat or shaping liveness still reaps dead clients).
perf.connection.idle_timeout_secs = 300
# New-session flood limit per source IP: at most N new sessions per window.
perf.connection.new_session_rate_max = 10
perf.connection.new_session_rate_window_secs = 60

# ── A second profile, e.g. UDP fake-TLS on :1443 (delete if unused). ────────
# [profile:udp]
# bind.port = 1443
# bind.transport = udp
# tun.name = vpn1
# tun.address = 10.1.0.1
# pool.cidr = 10.1.0.0/24
# obf.mode = fake-tls

# ── Users ───────────────────────────────────────────────────────────────────
# Users live in a SEPARATE file by default — `auth.users_file` above points at
# /etc/qeli/users.conf. Add them with:
#     qeli add-client <name> --config /etc/qeli/server.conf
# which appends the user to that file (see the shipped `users.conf` for the full
# [user:*] / [group:*] field reference).
#
# You CAN instead define users inline here as [user:<name>] sections, but then
# auth.users_file is IGNORED (inline takes precedence). Don't set both — the
# server warns and the file is silently dropped. The separate file is preferred.

# ── Optional group templates (inherited by users via `group = <name>`) ──────
# [group:premium]
# bandwidth_limit_mbps = 100
# max_sessions = 5
# allowed_networks = 0.0.0.0/0
