<Window x:Class="QeliWin.AppPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:l="clr-namespace:QeliWin"
        Title="{l:Loc AppsPickerTitle}" Width="560" Height="480"
        MinWidth="420" MinHeight="360"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource Bg}" FontFamily="{DynamicResource UiFont}">
    <Window.Resources>
        <!-- Row layout: checkbox + name/path. Avoids CheckBox.Content inside a
             horizontal StackPanel (infinite measure → horizontal scrollbar). -->
        <Style x:Key="AppRow" TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="6,5"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Stretch"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource Hover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel Margin="22">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="{l:Loc Cancel}" Click="OnCancel" MinWidth="104" Margin="0,0,10,0"/>
            <Button Content="{l:Loc Save}" Click="OnOk" Style="{StaticResource AccentButton}" MinWidth="130"/>
        </StackPanel>

        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <TextBlock Text="{l:Loc AppsPickerHint}" TextWrapping="Wrap"
                       Foreground="{DynamicResource FgDim}" FontSize="12.5" Margin="0,0,0,10"/>
            <TextBox x:Name="FilterBox" TextChanged="OnFilterChanged"/>
            <Button Content="{l:Loc AppsBrowse}" Click="OnBrowse" MinWidth="140"
                    HorizontalAlignment="Left" Margin="0,8,0,0"/>
        </StackPanel>

        <Border Background="{DynamicResource Panel}" BorderBrush="{DynamicResource PanelBorder}"
                BorderThickness="1" CornerRadius="8" Padding="4,4">
            <ListBox x:Name="AppsList"
                     Style="{StaticResource SlimListBox}"
                     ItemContainerStyle="{StaticResource AppRow}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox Grid.Column="0"
                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                      VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" Margin="10,0,4,0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}"
                                           Foreground="{DynamicResource Fg}" FontSize="13"
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding FullPath}"
                                           Foreground="{DynamicResource FgDim}" FontSize="11"
                                           TextTrimming="CharacterEllipsis" Margin="0,1,0,0"
                                           ToolTip="{Binding FullPath}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </DockPanel>
</Window>
