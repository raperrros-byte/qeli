<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Auto-connect the active profile after a reboot (opt-in in Settings). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- QR scanning for importing a qeli:// profile. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Enumerate installed apps for the per-app split-tunnel picker. Android 11+ (API 30)
         hides most packages behind a visibility filter, so without this the picker showed
         only a fraction of the device's apps. This build is sideloaded (GitHub Releases),
         not Play-distributed, so the QUERY_ALL_PACKAGES policy restriction does not apply. -->
    <!-- lintRelease flags this by default because the Play policy restricts it. The
         justification is the comment above: the per-app split tunnel cannot show an
         app list without it, and this build is not Play-distributed. Suppressed with
         that reasoning rather than left as a red gate nobody reads. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
                     tools:ignore="QueryAllPackagesPermission" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".QeliApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Qeli">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep link: tapping a qeli:// share link (from a messenger/browser) opens the
                 app and offers to import the profile. Pairs with the profile Share feature. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="qeli" />
            </intent-filter>
            <!-- Long-pressing the Quick Settings tile fires QS_TILE_PREFERENCES scoped to our
                 package; without an activity that resolves it, SystemUI falls back to the OS
                 App Info page. Handle it here so a long-press opens the app instead. -->
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- ZXing's stock CaptureActivity is full-screen. Qeli uses a bounded square camera
             dialog that follows the current device orientation instead. -->
        <activity
            android:name=".QrCaptureActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="false"
            android:theme="@style/Theme.Qeli.QrScanner" />

        <service
            android:name=".VpnServiceImpl"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.foreground_service.foreground_service_type_associated_models"
                android:value="vpn" />
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <!-- Quick Settings tile: one-tap connect the default (active) profile / disconnect. -->
        <service
            android:name=".QeliTileService"
            android:exported="true"
            android:icon="@drawable/ic_power"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Auto-connect on boot (opt-in). -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Home-screen widget: 1-tap connect/disconnect the active profile. Also listens
             to the service's package-scoped com.qeli.STATUS broadcast to stay in sync. -->
        <receiver
            android:name=".QeliWidgetProvider"
            android:exported="false"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.qeli.STATUS" />
                <action android:name="com.qeli.widget.TOGGLE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/qeli_widget_info" />
        </receiver>

    </application>

</manifest>
