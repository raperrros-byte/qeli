# Аудит 2026-07-27 — план устранения

Рабочий чек-лист по отчёту аудита от 2026-07-27. Ветка `dev`, база — `5b2bc68` (0.7.13, не выпущена).

**Статусы:** `[ ]` не начато · `[~]` в работе · `[x]` сделано · `[?]` требует решения владельца · `[-]` отклонено

**Проверяемость на машине разработчика:** локально доступны только `dotnet 10.0.300` и `java 24`.
`cargo`, `swift`, `gradle` отсутствуют — правки в Rust / Swift / Kotlin вносятся хирургически и
требуют прогона на лабораторной машине (`/opt/qeli-src` на .10) либо в CI до слияния.
Отметка `[x]` означает «правка внесена и вычитана», а не «собрано и протестировано»;
колонка «Проверка» фиксирует, чем именно это должно быть подтверждено.

---

## Порядок работ

1. Критичное (K) — блокирует выпуск 0.7.13.
2. Паники и переполнения на недоверенном вводе (F) — дёшево и снимает класс удалённых отказов.
3. Валидация (C) — включая единую схему диапазонов.
4. Teardown (B) и реестры UDP (A) — самые инвазивные, делать на свежую голову.
5. Маскировка (E) — требует отдельного conformance-набора, см. идею 6.1 отчёта.
6. Остальное (D, G, H, P, S, R, N, M, X, O).

---

## K — Критичное

| # | Что | Где | Статус | Проверка |
|---|-----|-----|--------|----------|
| K1 | iOS: `plain` не подключается — срез `Data` по абсолютным индексам | `qeli-ios/QeliPacketTunnel/PlainHandshake.swift:114` | [x] | ✅ `dropFirst(32).prefix(32)`; JOIN-путь наследует фикс через ту же `verifyServer` |
| K2 | Повторный запуск установщика затирает конфиг и обрывается | `install-qeli-server.sh:305,316,356` | [x] | прогон на чистой VM дважды |
| K3 | Supervisor затирает `usage.json` устаревшим снимком | `qeli/src/server/mod.rs:1482`, `usage.rs:213` | [x] | ✅ `load_read_only` + тест `read_only_handle_never_writes_the_file`; **тест проверен на отлов бага** (с отключённым фиксом FAILED) |
| K4 | JNI-мост без `catch_unwind` при `panic=unwind` | прежний Android JNI-мост (12 точек; удалён в TC-3.1) | [x] | ✅ старые 12 обёрток были защищены `guard`; после TC-3.1 действующие JNI-входы находятся в `qeli/src/transport_core/jni.rs` и также защищены от unwind; `cargo ndk -t arm64-v8a check` на .11 rc=0 |

## A — Два реестра UDP-сессий

| # | Что | Где | Статус | Проверка |
|---|-----|-----|--------|----------|
| A1 | Отказ по `max_clients` оставляет живой ingress | `qeli/src/server/udp_handler.rs:1247,1272,1343` | [x] | ✅ per-worker запись удаляется при отказе; e2e `test_maxsessions.py` ещё не гонялся |
| A2 | `kick` не обрывает входящий поток UDP | `qeli/src/server/control.rs:205`, `udp_handler.rs:1315` | [x] | ✅ флаг `SessionShared.revoked`, поднимает `kick_all`, проверяет цикл приёма; e2e `test_kick.py` ещё не гонялся |
| A3 | Отключение по квоте идёт тем же неполным путём | `qeli/src/server/mod.rs:1417` | [x] | ✅ закрыт тем же флагом (квота зовёт `kick_all`) |

## B — Teardown не выполняется на путях ошибок

| # | Что | Где | Статус | Проверка |
|---|-----|-----|--------|----------|
| B1 | Rust-клиент: маршруты + IPv6-blackhole утекают при падении `setup_tunnel` | `qeli/src/client/mod.rs:982,1036,3167` | [x] | ✅ откат маршрутов при провале `setup_routes` (guard создавался слишком поздно — в вызывающем коде) |
| B2 | C#: kill-switch не снимается, когда реконнект сдался | `qeli-shared/QeliShared/Vpn/VpnTunnelBase.cs:416` | [x] | ✅ `KillSwitchLift()`, `_ksEngaged` bool→int; сборка чистая, рантайм не прогонялся |
| B3 | C#: `Stop()` делает teardown до join'а | `qeli-shared/QeliShared/Vpn/VpnTunnelBase.cs:142` | [x] | ✅ две фазы: сокеты → join → TUN+`CleanupPlatform` |
| B4 | Android: зомби-сервис с удержанным wakelock | `qeli-android/.../QeliService.kt:411` | [x] | ✅ агент: `stopVpn()` на пути «сдались» |
| B5 | Сервер: TCP-сессия и маршруты утекают при сбое отправки AuthOK | `qeli/src/server/handler.rs:504-542` | [x] | ✅ полный откат (by_ip/by_token/iroute/пул) при сбое отправки AuthOK |
| B6 | Сервер: правила `forward_private` не снимаются | `qeli/src/server/mod.rs:1276` | [x] | ✅ `nat::cleanup` безусловно — тег `qeli-nat:<profile>` общий у NAT и `forward_private` |
| B7 | CLI C#: нет обработчика Ctrl+C | `qeli-win/.../CliRunner.cs:441`, `qeli-mac/.../CliRunner.cs:226` | [x] | ✅ `CancelKeyPress` / `PosixSignalRegistration` (SIGINT+SIGTERM) |

## C — Валидация есть в одном пути и отсутствует в параллельном

| # | Что | Где | Статус | Проверка |
|---|-----|-----|--------|----------|
| C1 | Пороги brute-force не проверяются в `PUT /api/config` | `qeli/src/web/api/config.rs:39,433` | [x] | `cargo test` |
| C2 | `put_config_raw` слабее `put_config` | `qeli/src/web/api/config.rs:433-548` | [x] | ✅ общий `validate_config_structure` (имена, дубликаты, маршруты) + `needs_full_restart` в raw-ответе |
| C3 | `IpPool::allocate` обходит проверки static-reservation | `qeli/src/server/pool.rs:143` | [x] | ✅ ветка идёт через `allocate_fixed`; тест `unusable_static_reservation_falls_back_to_dynamic` |
| C4 | `tun.mtu` — три разных диапазона | `config/server_ini.rs:660`, `server/mod.rs:986`, `client/mod.rs:2029` | [x] | ✅ `MTU_MIN`/`MTU_MAX` + `mtu_in_range()` — один источник для сервера, клиента и ссылки |
| C5 | `client_ip`/`server_ip` из AuthOK не валидируются | `qeli/src/client/mod.rs:2012,2035` | [x] | ✅ обе величины парсятся как Ipv4Addr; тест `parse_auth_ok_validates_pushed_addresses` |
| C6 | `mtu`/padding из ссылки не клампятся в C# и Kotlin | `VpnConfig.cs:569,680`, `Config.kt:344,539` | [x] | ✅ Kotlin-половина закрыта агентом |
| C7 | Дубли inline `[user:*]` не дедуплицируются | `qeli/src/config/server_ini.rs:118` | [x] | ✅ дедуп first-wins в `ServerConfig::from_ini` + отказ на дубликат в `put_config`; тест |
| C8 | `short_id_from_hex` вырождается в нули | `qeli/src/crypto/reality.rs:48` | [x] | ✅ `parse_short_id` + allow-list + отказ при старте, 2 теста |
| C9 | `dhcp.pool_*` не проверяется на принадлежность подсети | `qeli/src/server/mod.rs:1016,2341` | [x] | ✅ `dhcp_pool_bounds()` — дефолт из подсети туннеля + проверка вхождения; 3 теста |
| C10 | iOS: шейпинг из AuthOK не клампится, `Int(Double)` трапает | `qeli-ios/QeliCore/Protocol/TrafficShaper.swift:34` | [x] | ✅ кламп в Double до конверсии + `clampPushedObfuscation()` вызывается из обоих парсеров AuthOK |

## D — Расщепление supervisor/worker

| # | Что | Где | Статус | Проверка |
|---|-----|-----|--------|----------|
| D1 | Смена `auth.users_file` не доходит до панели | `qeli/src/web/api/config.rs:287` | [x] | ✅ `auth.users_file` учтён в `needs_full_restart` |
| D2 | Живая перезагрузка web-настроек слабее стартового гейта | `qeli/src/server/mod.rs:665` | [x] | ✅ живая перезагрузка применяет то же правило, что стартовый гейт, + повторный WARN |

## E — Маскировка слабее заявленной

| # | Что | Где | Статус | Проверка |
|---|-----|-----|--------|----------|
| E1 | WS отвечает `101` на любой запрос + случайный accept | `qeli/src/protocol/obfs.rs:242` | [x] | ✅ строгая проверка апгрейда по RFC 6455 §4.2.1 + правдоподобный 400; тест |
| E2 | WS `Host` из пула приманок, не связан с dst | `qeli/src/protocol/obfs.rs:208` | [x] | ✅ `Host` = хост подключения (та же логика, что у SNI); тест |
| E3 | Нет `Pong` на `Ping`, `Close` не обрабатывается | `qeli/src/protocol/obfs.rs:423` | [x] | ✅ Pong на Ping, эхо Close; общая очередь между половинами; тест |
| E4 | QUIC: Handshake с полем Token Length, нет Initial | `qeli/src/protocol/quic.rs:19-52` | [x] | ✅ тип пакета Initial (0x00) вместо Handshake — Token Length теперь легален; детекция не зависит от типа |
| E5 | ClientHello: пустой padding после GREASE | `qeli/src/protocol/realtls/clienthello.rs:222` | [-] | **ОТОЗВАНА** — тест `ja4_matches_chrome` фиксирует опубликованный отпечаток `t13d1516h2`, где padding и есть шестнадцатое расширение. Порядок относительно GREASE JA4 не различает; нужны байтовые векторы (см. I1) |
| E6 | Android: cover-трафик не из CSPRNG | `qeli-android/.../TrafficShaper.kt:56` | [x] | ✅ агент: `java.security.SecureRandom` напрямую (не seeded-XorWow) |
| E7 | Padding капается константой 1400 вместо `tun.mtu` | `qeli/src/server/mod.rs:2216` | [x] | ✅ кап от `tun.mtu` профиля вместо константы 1400 |
| E8 | `generate_padding` при `min>=max` отдаёт нули | `qeli/src/protocol/obfuscate.rs:18` | [x] | ✅ случайное содержимое на всех путях + тест |

## F — Паники и переполнения на недоверенном вводе

| # | Что | Где | Статус | Проверка |
|---|-----|-----|--------|----------|
| F1 | `parse_pubkey_hex` паникует на не-ASCII длиной 64 байта | `qeli/src/crypto/auth.rs:159` | [x] | ✅ тест `parse_pubkey_hex_rejects_non_ascii_without_panicking` |
| F2 | Переполнение вычитания в реаперах idle/rx-dead | `qeli/src/server/handler.rs:1061,1065` | [x] | ✅ `cargo test` 390 зелёных |
| F3 | `RecordCrypto::encrypt` не ограничивает размер записи | `qeli/src/protocol/realtls/record.rs:86` | [x] | ✅ фрагментация по `MAX_PLAINTEXT` (RFC 8446 §5.1), а не обрезка |
| F4 | Усечение длины WS-фрейма `u64 as usize` на 32-бит | `qeli/src/protocol/obfs.rs:398-408` | [x] | ✅ сравнение в u64 до сужения |
| F5 | QUIC Length varint защищён только `debug_assert` | `qeli/src/protocol/quic.rs:36` | [x] | ✅ `push_varint` выбирает 1/2/4-байтовую форму, `read_varint` их уже принимал |
| F6 | iOS: счётчик ChaCha20 оборачивается вместо отказа | `qeli-ios/QeliCore/Protocol/ObfsPrimitives.swift:117` | [x] | ✅ `xor` бросает `counterExhausted`; все 4 вызова уже были в throws-контексте |
| F7 | Android: случайный AEAD-nonce (M6 не применён) | `qeli-android/.../protocol/PacketCodec.kt:113` | [x] | ✅ приехало параллельной работой; агент проверил PRP-вектора независимой реализацией на Python |
| F8 | Мёртвый `ParseServerHello` с выходом за границу | `TlsHandshake.cs:191`, `TlsHandshake.kt:135` | [x] | ✅ Kotlin-половина закрыта агентом |

## G — Провенанс сборок

| # | Что | Где | Статус | Проверка |
|---|-----|-----|--------|----------|
| G1 | Нативные ядра собраны из 0.7.12, дерево 0.7.13 | `native-libs/` | [x] | ✅ ядра пересобраны из текущего дерева (win/mac на .10, android на .11); 12 JNI-символов на месте, манифест сходится |
| G2 | CI не собирает и не ставит публикуемый `.deb` | `.github/workflows/ci.yml` | [x] | зелёный CI |
| G3 | `.cargo/config.toml` не применяется к zigbuild-триплету | `qeli/.cargo/config.toml:2` | [x] | `make check-abi` |
| G4 | `app.manifest` застрял на 0.7.11.0 | `qeli-win/QeliWin/app.manifest:3` | [x] | `sync_version.py` |
| G5 | OpenWrt: git-источник без `PKG_MIRROR_HASH` | `qeli-openwrt/Makefile:14` | [~] | `make package/qeli/check` |
| G6 | `release-attest.yml` подписывает несверенные ассеты | `.github/workflows/release-attest.yml:62` | [x] | зелёный workflow |

## H — Документация описывает другую систему

| # | Что | Где | Статус |
|---|-----|-----|--------|
| H1 | ~~THREAT-MODEL занижает защищённость~~ — **находка ОТОЗВАНА, см. журнал.** Документ прав; исправлен вводящий в заблуждение комментарий в `Cargo.toml` | `qeli/Cargo.toml:55` | [x] |
| H2 | Параметры Argon2 указаны неверно и не зафиксированы в коде | `docs/{eng,ru}/AUDIT.md:15` + код | [x] |
| H3 | Сайт: Keenetic/OpenWrt «проверены на реальных роутерах» | `site/index.html:462,474`, `site/support/index.html:68` | [x] |
| H4 | README: «binaries are not committed to git» | `README.md:87` | [x] |
| H5 | Сайт документирует несуществующий `exclude_routes` | `site/install/index.html:475` | [x] |
| H6 | Сайт: «по умолчанию reality-tls» (дефолт конфига — fake-tls) | `site/index.html:213`, `site/tech/index.html:111` | [x] |
| H7 | Сайт: `curl \| bash` вопреки README | `site/install/index.html:94` | [x] |
| H8 | Профиль `udp-quic` не задокументирован | `README.md:52`, `docs/*/GETTING-STARTED.md` | [x] |
| H9 | AUDIT.md: число тестов, состав CI, Basic-Auth | `docs/{eng,ru}/AUDIT.md` | [x] |
| H10 | SECURITY.md «nftables»; LICENSING.md обещает license-scan | `SECURITY.md:58`, `LICENSING.md:61` | [x] |
| H11 | RELEASE-FIXES: B3 всё ещё открыт | `docs/{eng,ru}/RELEASE-FIXES.md` | [x] |
| H12 | `check_docs.py` не покрывает клиентские ключи | `scripts/check_docs.py:134` | [x] |
| H13 | Мелочи: версия в футере сайта, даты бенчмарков, jemalloc в CONTRIBUTING, MSRV | разное | [x] |

## P — Веб-панель (прочее)

| # | Что | Где | Статус |
|---|-----|-----|--------|
| P1 | `GET /api/config/raw` отдаёт хеш админа и секреты пользователей | `qeli/src/web/api/config.rs:421` | [x] |
| P2 | Команда обновления из непроверенного JSON GitHub в root-шелл | `qeli/src/web/templates/layout.html:294` | [x] |
| P3 | CSRF доверяет loopback-Origin на любом порту | `qeli/src/web/mod.rs:226` | [x] |
| P4 | Нет `Cache-Control: no-store` на API и страницах | `qeli/src/web/mod.rs:434` | [x] |
| P5 | Сохранение конфига теряет ключи чужого транспорта | `qeli/src/config/server_ini.rs:592,606` | [x] |
| P6 | `logging.format` и anti-fingerprinting сохраняются, но не читаются | `config/server_ini.rs:338,838` | [x] |

## S — Сервер (прочее)

| # | Что | Где | Статус |
|---|-----|-----|--------|
| S1 | Глобальный mutex FFI-реестра держится на время AEAD | `qeli/src/protocol/realtls/registry.rs:95` | [x] |
| S2 | `notify::fire` блокирующе читает файл даже при выключенных уведомлениях | `qeli/src/server/notify.rs:137` | [x] |
| S3 | Путь control-сокета захардкожен при наличии `--socket` | `qeli/src/server/control.rs:9` | [x] |
| S4 | Метрики WAN фильтруют по префиксам имён, не по `tun.name` | `qeli/src/server/metrics.rs:298` | [x] |
| S5 | Лог клиентского туннеля открывается с `truncate` | `qeli/src/server/client_manager.rs:154` | [x] |
| S6 | Несогласованная обработка отравленного `RwLock` | `qeli/src/server/mod.rs:2057` | [x] |

## R — Rust-клиент (прочее)

| # | Что | Где | Статус |
|---|-----|-----|--------|
| R1 | `rp_filter` на tun — тихий no-op (engage до создания TUN) | `qeli/src/client/gateway.rs:232,427` | [x] |
| R2 | RX-детект мёртвой линии выключается без heartbeat и shaping | `qeli/src/client/mod.rs:876` | [x] |
| R3 | `refresh_server_ips` только добавляет разрешения | `qeli/src/client/killswitch.rs:363` | [x] |
| R4 | WAN-интерфейс определяется зондом к захардкоженному 1.1.1.1 | `qeli/src/client/gateway.rs:95` | [x] |
| R5 | Захардкоженные резолверы в фолбэке и восстановлении | `qeli/src/client/dns.rs:74,455` | [x] |
| R6 | `exclude` удаляет чужой маршрут без журналирования | `qeli/src/client/route.rs:293` | [x] |
| R7 | `RESOLVECTL_MARK` — один файл на все инстансы | `qeli/src/client/dns.rs:31` | [x] |
| R8 | `load_or_create_key`: незалоченный дублирующий путь | `qeli/src/crypto/secret.rs:45` | [x] |

## N — C#-клиенты (прочее)

| # | Что | Где | Статус |
|---|-----|-----|--------|
| N1 | Гонка на несинхронизированном `List<Task> jobs` | `VpnTunnelBase.cs:2157,2191,2356` | [x] |
| N2 | Server-pushed padding не капается на TCP | `VpnTunnelBase.cs:1363,1687,1800` | [x] |
| N3 | macOS: kill-switch перезаписывает главный pf-ruleset | `qeli-mac/QeliMac/Vpn/KillSwitch.cs:196` | [x] |
| N4 | Windows: `Stop()` на UI-потоке при выходе | `qeli-win/QeliWin/MainWindow.xaml.cs:335,156` | [x] |
| N5 | `TestHandshake` необратимо переводит объект в handshake-only | `VpnTunnelBase.cs:125` | [x] |
| N6 | Windows: ожидание на освобождённом event-хендле | `qeli-win/QeliWin/Vpn/Wintun.cs:151` | [x] |

## M — Мобильные клиенты (прочее)

| # | Что | Где | Статус |
|---|-----|-----|--------|
| M1 | Android: Always-on VPN не работает вообще | `qeli-android/.../QeliService.kt:193` | [x] |
| M2 | Android: любая не-дефолтная сеть рвёт туннель, `onLost` не обрабатывается | `QeliService.kt:471` | [x] |
| M3 | Android: устаревший цикл ретраев закрывает сокеты новой сессии | `QeliService.kt:338,390,407` | [x] |
| M4 | iOS: `startTunnel` рапортует успех при отменённом туннеле | `QeliTunnelEngine.swift:69` | [x] |
| M5 | iOS: `setTunnelNetworkSettings` без таймаута под неотменяемым gate | `QeliTunnelEngine.swift:245,1302` | [x] |
| M6 | iOS: uplink-таск неотменяем, `provider` — `unowned` | `QeliTunnelEngine.swift:1066,19` | [x] |
| M7 | iOS: IPv6-blackhole игнорирует `allowLAN` | `QeliTunnelEngine.swift:216` | [x] |
| M8 | iOS: `updateOnDemand` молча не сохраняет при выключенном менеджере | `TunnelManager.swift:176` | [x] |
| M9 | iOS: MDM-запрет виджет-контролов fail-open | `WidgetControlBridge.swift:109` | [x] |
| M10 | iOS: версия захардкожена 0.7.12/715 | `QeliCore/AppConstants.swift:4` | [x] |
| M11 | Android: `mtu` из ссылки/INI не проверяется на диапазон | `Config.kt:344,539` | [x] |

## X — Мёртвый код с расходящейся семантикой

| # | Что | Где | Статус |
|---|-----|-----|--------|
| X1 | `Cipher::generate_nonce` + тест закрепляют неверный формат | `qeli/src/crypto/cipher.rs:23`, `crypto/mod.rs:206` | [x] |
| X2 | `generate_heartbeat` формирует TLS Heartbeat (демаскирующий признак) | `qeli/src/protocol/obfuscate.rs:134` | [x] |
| X3 | `QUIC_LONG_HEADER_MIN` = 12 при реальных 18 | `qeli/src/protocol/quic.rs:10` | [x] |
| X4 | Kotlin `toConfigJson` не вызывается, жёстко пишет full-tunnel | `qeli-android/.../model/Config.kt:168` | [x] |

## O — Операционное

| # | Что | Где | Статус |
|---|-----|-----|--------|
| O1 | `systemctl enable --now` не перезапускает работающий сервис | `install-qeli-server.sh:434` | [x] |
| O2 | Юнит и конфиг-примеры качаются с `main` без проверки целостности | `install-qeli-server.sh:58-65` | [x] |
| O3 | `$PUBLIC_HOST` без валидации попадает в `sed` | `install-qeli-server.sh:427` | [x] |
| O4 | Панель на `0.0.0.0` без allowlist; мёртвый `sed` для TLS | `install-qeli-server.sh:421-423` | [x] |
| O5 | Сбой iptables прерывает установку; `iptables-save` затирает ruleset | `install-qeli-server.sh:384-396` | [x] |
| O6 | Откат апдейта не откатывает dpkg | `update-qeli-server.sh:303` | [x] |
| O7 | Мёртвые lab-скрипты с боевыми паттернами | `scripts/{deploy-server,fix-and-test,deploy-vpn,diagnose}.sh` | [x] |
| O8 | `release_preflight.py` собирает несинхронизированное дерево | `scripts/release_preflight.py:75` | [x] |

## I — Идеи из раздела 6 отчёта (крупные)

| # | Что | Статус |
|---|-----|--------|
| I1 | Conformance-набор для маскировки (`conformance/wire-vectors/`) | [ ] |
| I2 | Единая декларативная схема валидации конфигурации | [ ] |
| I3 | Единый реестр UDP-сессий (закрывает A целиком) | [ ] |
| I4 | Провенанс нативных ядер (`PROVENANCE` + проверка в CI) | [x] |
| I5 | Тесты для непокрытых модулей — **уточнено:** `crypto/` покрыт (51 тест), дыры это `web/api/*`, `config/server.rs` (671 стр., 0), `client/gateway.rs` (539, 0), `web/auth.rs` (340, 0) | [ ] |
| I6 | Разукрупнение god-модулей (`client/mod.rs`, `server/mod.rs`) | [?] отдельная задача, не в этом проходе |
| I7 | Внешний аудит realtls | [?] организационное решение |

---

## Z — Найдено в процессе устранения (не было в отчёте)

| # | Что | Где | Статус |
|---|-----|-----|--------|
| Z1 | **Гейт `lint` в CI красный на текущем HEAD.** `cargo fmt --check` не проходит на `config/client.rs` — два `assert!` в тестах, приехавших с коммитом 0.7.13 `5b2bc68`. CI выполняет проверку в `qeli/` (`defaults.run.working-directory`), так что job падает | `qeli/src/config/client.rs:1237,1253` | [x] |
| Z2 | Предупреждение о MITM не доходит до пользователя: ветка MITM ставит `Status(Error, MitmStop)` и делает `break`, после чего хвост «сдались» немедленно перезатирает его на `CouldNotConnect` | `qeli-shared/QeliShared/Vpn/VpnTunnelBase.cs` (ветка MITM в `ConnectWithRetry`) | [x] |
| Z3 | Windows: при завершении сеанса ОС (logoff/shutdown) `OnWindowClosing` выполняется с `_exiting == false`, ставит `e.Cancel = true` (при shutdown игнорируется) — туннель не разбирается вообще | `qeli-win/QeliWin/MainWindow.xaml.cs` | [x] |
| Z4 | Цифры производительности на сайте отстали на два релиза: `reality-tls` показан как 482 ↑ / 417 ↓ (прогон 0.7.0), тогда как BENCHMARK.md фиксирует ~518 ↑ / ~430 ↓ начиная с 0.7.4. Атрибуция исправлена, сами числа — нет | `site/index.html`, `site/tech/index.html` | [x] |
| Z6 | `check_docs.py` считал ссылкой markdown-разметку ВНУТРИ code-span: тип `[UInt8](raw)` в тексте ломал doc-гейт. Ни один рендерер так не делает | `scripts/check_docs.py:77` | [x] |
| Z5 | `fuzz-smoke` — единственный job с `continue-on-error`, при том что комментарии называют «мягкими гейтами» и клиентские сборки. Формулировка в CI расходится с механикой | `.github/workflows/ci.yml` | [x] |

---

## Репорт с эксплуатации (2026-07-25) — шесть пунктов с боевых установок

Отдельный список: это не находки аудита, а то, с чем столкнулись на реальных серверах.
Диагностика каждого вынесена в `docs/{ru,eng}/TROUBLESHOOTING.md` §6.9–6.12 — там же
команды для установок, сломанных ещё до апдейта.

| # | Симптом | Статус |
|---|---------|--------|
| 1 | Windows: после сна ~1 мин до реконнекта, иногда трафик уходит мимо туннеля | [x] Бэкофф больше не эскалирует на попытках в неподнявшуюся сеть (`VpnTunnelBase`) |
| 2 | Пользователь `qeli` теряет права на `/etc/qeli/users.conf.lock` — не выпускается QR/ссылка | [x] `write_atomic` сохраняет владельца; **сломанным установкам нужен разовый `chown -R`** |
| 3 | `create temp in /etc: Read-only file system (os error 30)` | [x] Закрыто ранее в этом релизе (R8) |
| 4 | В докере рестарт не происходит, `systemctl` не найден | [x] Закрыто ранее (`control.rs`, `kind: container`) |
| 5 | После сохранения настроек в панели службу приходится рестартить руками | [x] Закрыто ранее (`kind: polkit_missing` + `qeli install-polkit`) |
| 6 | `resolvectl` «не виден», хотя systemd-resolved уже установлен | [x] Сообщение врало: наличие бинарника никогда не проверялось |

## Журнал

| Дата | Что сделано |
|------|-------------|
| 2026-07-28 | **Z7 — гейт нативных библиотек не делал того, что декларировал; поймано на себе.** Пересборка ядер после правок этого дня показала, что `native-libs/android/*` и `jniLibs/*` — РАЗНЫЕ файлы по обеим ABI, при том что `verify.sh` рапортовал OK. Причина: манифест пишет хеш каждого пути отдельно, и `sha256sum -c` сверяет каждый файл сам с собой — пара не сравнивалась НИКОГДА, хотя ровно это заявлено в шапке скрипта как его вторая задача. Механизм дрейфа: `scripts/build_android_so_11.py` копирует только в `jniLibs`, поэтому каждая пересборка Android оставляла каноническую копию устаревшей, а `--update` молча записывал два разных хеша и закреплял расхождение как правильное — то есть **я сам благословил дрейф в предыдущую пересборку** (журнал выше, «ядра пересобраны… manifest пересчитан»). Исправлено втроём: копии сведены (авторитетна `jniLibs` — туда пишет сборка), `verify.sh` переведён на список ПАР с побайтовым `cmp` и отказом `--update` при расхождении, сборочный скрипт теперь пишет в обе копии. Негативно проверено в обе стороны: подмена байта в канонической копии даёт `DRIFT` + `MISMATCH`, а `--update` отказывается запускаться. |
| 2026-07-28 | **Репорт с эксплуатации: 1, 2, 6 закрыты** (3, 4, 5 были закрыты раньше по ходу аудита). По №2 корневая причина оказалась глубже симптома: дело не в файле блокировки, а в том, что `write_atomic` сохранял режим, но не владельца — `rename` подставляет новый inode, принадлежащий тому, кто пишет, поэтому один `sudo qeli add-client` переводил `users.conf` в `root:root`, а блокировка, создаваемая с владельцем охраняемого файла, наследовала это. Тест `atomic_write_preserves_owner` негативно проверен на лаборатории: без фикса `uid` = 0 вместо 65534; помечен `#[ignore]`, потому что требует root. По №1 арифметика сошлась с репортом: база 1с, удвоение на попытку, 1+2+4+8+16+32 = 63с — «около минуты» ровно тогда, когда несколько попыток сгорают в ещё не поднявшуюся сеть; сюда же вторая половина жалобы, так как исчерпание `max_retries` снимает TUN и маршруты. Счётчик попыток ограничен на 30с после пробуждения; окно взводит и power-хук, и детектор дрейфа часов. По №6 правился только текст: логика выбора пути DNS верна (важно, кто фактически резолвит, а не наличие бинарника), врало сообщение — и оно же отправляло администратора чинить единственное, что не было сломано. **C#-тестового проекта в дереве нет, поэтому правка бэкоффа покрыта только сборкой** (три проекта, 0 предупреждений); прод-проверка — лог от пробуждения до `Connected`. Гейт: `fmt` 0, `clippy -D warnings` 0, **405 тестов + 1 ignored**, `check_docs` 7/7. |
| 2026-07-27 | План создан. |
| 2026-07-27 | Поднят цикл проверки на лаборатории (.10): `scratchpad/lab.py push` + `cargo test/fmt/clippy`. **Базовая линия: 387 тестов, 0 падений.** |
| 2026-07-27 | **H1 отозвана.** Проверка на .10 показала: у `aes-gcm 0.10.3` нет секции `zeroize` в `[features]` — это неявная фича опциональной зависимости, защищающая ровно одну строку `ghash_key.zeroize()` (16-байтовый временный GHASH-подключ). Расширенный AES key schedule она не трогает, а у крейта `aes 0.8.4` переносимой фичи `zeroize` нет вовсе (зависимость объявлена только под `cfg(aes_armv8, aarch64)`). Значит THREAT-MODEL §4 **прав**, а неточен был комментарий в `Cargo.toml`, приписывавший фиче зануление key schedule. Комментарий переписан по факту. |
| 2026-07-27 | **Уточнение по покрытию тестами.** В отчёте (идея 6.5) утверждалось, что `crypto/` почти не покрыт — это следствие обрезанного через `tail` списка. Фактически в `crypto/` 51 тест; без тестов остаются `web/api/*`, `config/server.rs`, `client/gateway.rs`, `web/auth.rs`, `server/metrics.rs`, `tun/iface.rs`, `realtls/jni.rs`. |
| 2026-07-27 | Партия 1 (Rust): F1, F2, F4, C8, Z1. Гейт зелёный — `fmt` чист, `clippy -D warnings` чист, **390 тестов** (было 387, добавлено 3). |
| 2026-07-27 | Партия 2 (Rust): F3 (фрагментация записи по RFC 8446 §5.1), F5+X3 (`push_varint`, корректный размер заголовка), E8, S6. Гейт зелёный, **391 тест**. |
| 2026-07-27 | **K4** — все 12 точек входа JNI обёрнуты в `catch_unwind` через хелпер `guard`. Проверено кросс-компиляцией на .11: `cargo ndk -t arm64-v8a check --lib` rc=0. |
| 2026-07-27 | **K3** — `UsageStore::load_read_only` для supervisor'а; `flush` и `Drop` у read-only копии — no-op, корректный файл при старте не переименовывается. Добавлен регрессионный тест, **проверено что он ловит баг**: с отключённым фиксом `read_only_handle_never_writes_the_file` падает на «read-only flush must not touch the file». **392 теста**. |
| 2026-07-27 | **Тема A целиком (A1/A2/A3)** — введён `SessionShared.revoked` (`Arc<AtomicBool>`): поднимается в `kick_all()`, проверяется в UDP-цикле приёма до траты AEAD, запись per-worker удаляется. Отдельно A1: при отказе по `max_clients` теперь удаляется и per-worker запись, а не только резервация IP. Гейт зелёный. |
| 2026-07-27 | **H2 (кодовая часть)** — добавлен `crypto::password_hasher()` с явными параметрами Argon2id (m=19456, t=2, p=1 — те же, что дефолт крейта, поэтому поведение не меняется). Переведены 4 места хеширования + dummy-хеш для выравнивания тайминга (иначе рост стоимости вернул бы оракул имён). Места ПРОВЕРКИ намеренно оставлены на `Argon2::default()` — параметры берутся из PHC-строки. |
| 2026-07-27 | Агент по документации: **H2–H13 закрыты**, `check_docs.py` расширен экстрактором клиентских ключей (39 шт.) и префиксных ключей, негативно протестирован. Поправки к отчёту: мест вызова `Argon2::default()` **восемь**, а не девять (девятый хит — `CertificateParams::new`); прогона бенчмарка 0.7.12 в дереве нет, свежайший — 0.7.11; каталог `site/` в `.gitignore`, его правки не видны в `git status`. |
| 2026-07-28 | Партия 3 (Rust): **C1** (`BruteForceConfig::validate`, вызывается из `validate_profiles` — то есть закрыто сразу для `check-config`, старта, `PUT /api/config` и `PUT /api/config/raw`; панельный валидатор делегирует туда же), **C3**, **C4**. Гейт зелёный, **393 теста**. |
| 2026-07-28 | Агент по инсталляторам/CI **упал на лимите сессии**, но успел закрыть K2, O1–O8, G2–G4, G6 и частично G5. Проверено вручную: YAML валиден, добавлены job'ы `deb-package` (сборка настоящего `.deb` + `check-abi` + установка в `debian:10`) и `shellcheck`, в матрицу keenetic добавлен `x86_64-musl`; четыре OBSOLETE-скрипта удалены; `check_docs.py` и `sync_version.py` проходят. **Исправлено за ним:** он записал два workflow-файла в CRLF — нормализовано в LF. **G5 остался частичным:** `PKG_MIRROR_HASH` — плейсхолдер из нулей с TODO, нужен реальный хеш на релизе, и `PKG_SOURCE_VERSION` указывает на `5b2bc68`, то есть на коммит до всех этих правок. |
| 2026-07-28 | Партия 4 (Rust): **B5, B6, C5, C7, C9, D1, D2**. Гейт зелёный, **398 тестов**. |
| 2026-07-28 | Партия 5 — **блок маскировки E1–E3**. E1: сервер требует корректный WS-апгрейд (GET + `Upgrade` + `Connection: upgrade` + `Sec-WebSocket-Key` из 16 байт base64) и отвечает обычным `400 Bad Request` вместо `101`; случайный `Sec-WebSocket-Accept` убран. E2: заголовок `Host` берётся из хоста подключения по той же логике, что и SNI (`obfuscation.sni` → адрес сервера → приманка только для голого IP); добавлен `connect_with_host`, семь тестовых вызовов не тронуты. E3: `Ping` получает `Pong` с эхом payload, `Close` — однократное эхо; очередь control-фреймов общая между половинами через `Arc<Mutex<…>>`, потому что путь чтения не имеет доступа на запись. **401 тест**, `fmt` и `clippy -D warnings` чисты. |
| 2026-07-28 | Все правки внесены в CHANGELOG.md — 12 секций в начале 0.7.13, сгруппированных по типу отказа, а не по идентификаторам находок. Отдельно выделена секция «валидация стала строже (требует внимания)»: конфиги с mtu вне 576..=9000, с нулевыми порогами brute-force и с DHCP-пулом вне подсети туннеля теперь ОТВЕРГАЮТСЯ при загрузке, а раньше принимались и ломались молча. |
| 2026-07-28 | **Гейт провенанса сработал на мне самом.** После пересборки ядер я правил ещё Rust (S3 + фикс clippy), плюс приехала параллельная conformance-работа — `provenance.py --check` вернул STALE. Ядра пересобраны повторно, оба манифеста обновлены. Итог: `fmt` 0, `clippy -D warnings` 0, **405 тестов**, три C#-проекта без ошибок, selftest ALL PASS, `check_docs` / `sync_version` / `verify.sh` / `provenance` — все rc=0. |
| 2026-07-28 | **Z4 отозвана — третья неверная находка.** Цифры сайта (`482 ↑ / 417 ↓`) точно соответствовали строке 0.7.0 в BENCHMARK.md, и атрибуция это называла. «Свежие» прогоны не образуют ряда: 518/418 → 549/431 → 500/443 → 513/451 → 499/421, а самый последний (0.7.11 vs 0.7.9) — 489/392, по download НИЖЕ показанного. Подстановка любого была бы выбором удобного числа. **Но настоящая проблема там нашлась другая:** сайт выносил в заголовок медиану с σ 99 и разбросом 437–688 как «~482», при том что BENCHMARK.md прямо пишет, что 0.7.1 это вылечил (σ 99 → 19). Заменил строку reality-tls на 0.7.1 (518 / 418) — не «посвежее», а на ту, которую проект признаёт стабильной; атрибуция переписана соответственно. |
| 2026-07-28 | **Z5** — комментарии CI врали в двух местах: клиентские сборки названы «soft gate», хотя `continue-on-error` у них нет (единственный реально мягкий job — `fuzz-smoke`), а у `security-audit` внешний комментарий говорил «non-gating, повысить позже», внутренний — что уже повышен. Формулировки приведены к механике. |
| 2026-07-28 | **Z6 (новая находка, поймана своим же файлом плана).** `check_docs.py` искал ссылки во ВСЁМ тексте, включая code-span: `[UInt8](raw)` в обратных кавычках сломал doc-гейт. Ни один markdown-рендерер так не считает. Добавлен `strip_code()` перед поиском ссылок. Негативно проверен: настоящая битая ссылка вне кода по-прежнему ловится. |
| 2026-07-28 | **G1 закрыт и стал критичным по ходу работы.** На момент аудита разница между 0.7.12-ядром и деревом была косметической. К этому моменту в FFI-ядро входят S1 (параллелизм реестра — то, ради чего существует бондинг), F1, F3, E1–E4 — то есть без пересборки GUI-клиенты не получали НИ ОДНОГО из этих исправлений. Ядра пересобраны из текущего дерева: win/mac на .10, android на .11, `CARGO_PROFILE_RELEASE_PANIC=unwind`. Проверено: 6 C-ABI символов в каждом, **12 `Java_com_qeli_*`** в Android-`.so` (ровно те точки входа, что обёрнуты в K4). Манифест пересчитан, `verify.sh` сходится, Windows-клиент пересобран. |
| 2026-07-28 | **I4 сделан заодно** — `native-libs/PROVENANCE` + `native-libs/provenance.py`: дайджест SHA256 по всем `qeli/src/**/*.rs` + `Cargo.toml`/`Cargo.lock`. Отвечает на вопрос, на который `verify.sh` ответить не может («соответствует ли бинарь исходнику рядом»). Встроен шагом в CI-job `native-libs`. Негативно протестирован: правка `lib.rs` → STALE, откат → OK. |
| 2026-07-28 | Агент по Android закрыл **M1, M2, M3, M11, B4, E6, X4** и Kotlin-половины **C6/F8**; F7 уже приехал параллельной работой и был им независимо проверен (реализация Feistel-PRP на Python против фикстур). Гейт на .11: `compileReleaseKotlin` + `testDebugUnitTest` (**31 тест**) + `lintRelease` — зелено, новый тест C6 негативно проверен. |
| 2026-07-28 | Партия 11: **S3** и **Z3**. S3: отдельной серверной секции в конфиге нет, класть путь сокета в `[auth]`/`[web]` семантически неверно, а атрибут `env` у clap требует не подключённой фичи — поэтому переопределение через `QELI_CONTROL_SOCKET` и вычисляемый `default_value_t` у флага. Z3: `SessionEnding` — единственное событие, которое реально приходит при logoff/shutdown; teardown синхронный (ОС даёт ограниченное окно), но на рабочем потоке с ожиданием 10 с, иначе повторился бы Dispatcher-дедлок из N4. |
| 2026-07-28 | **Замечено чужое изменение в дереве:** появились фича `conformance-gen`, бинарь `gen-conformance` и KAT-тесты в `protocol/packet.rs` — это в русле идеи I1, не трогал. Пришлось только починить за ними гейт: файл `gen_conformance_main.rs` не проходил `cargo fmt`, а `packet.rs:486` падал на `clippy -D warnings` (`manual implementation of is_multiple_of`). Оба CI-джоба (`lint`) на этом бы упали. |
| 2026-07-28 | Партия 10: iOS — **M4, M5, M6**; C# — **Z2**. M5: task-group здесь не работает (коллбэк-мост неотменяем, группа всё равно ждала бы его), поэтому продолжение резюмирует сам таймер под одноразовым guard. M6: первая версия правки была **неверной** — `onCancel` не видит продолжение и в таком виде вешала бы таск намертво; переписано через бокс, где продолжение доступно обеим сторонам. C#: сборка трёх проектов чистая, selftest ALL PASS. |
| 2026-07-28 | Партия 9: ядро — **B1, C2, P2, P5, P6**; iOS — **K1, C10, F6, M7, M8, M9, M10**. По K1 дополнительно проверил весь iOS на тот же класс ошибки (срез `Data` с абсолютными индексами): второе совпадение в `PacketCodec.prpNonce` безопасно, там `[UInt8](raw)` даёт свежий 0-базовый массив. `PlainJoinHandshake` наследует фикс, так как зовёт ту же `verifyServer`. **⚠️ iOS собрать негде** — Swift есть только на macOS; правки внесены хирургически и требуют сборки перед выпуском. Ядро: **404 теста**, `fmt` и `clippy -D warnings` чисты. |
| 2026-07-28 | Партия 8 (клиент): **R1–R8 закрыты полностью.** R1 — `rp_filter` на tun применяется теперь ПОСЛЕ создания интерфейса (раньше `engage`/`engage_exit` писали в `/proc/sys/.../conf/<tun>/` до того, как путь существовал, и молча ничего не делали; ядро берёт `max(conf/all, conf/<iface>)`, поэтому строгий RPF на tun оставался и рубил ровно тот асимметричный трафик, ради которого включают gateway-NAT). R2 — RX-детект мёртвой линии больше не отключается без heartbeat и shaping (иначе разрыв замечался только по TCP-retransmit, ~15 минут). R3 — kill-switch отзывает разрешения для адресов, которые больше не резолвятся: новые вставляются ДО удаления старых, поэтому окно утечки не появляется. R4 — WAN-интерфейс берётся из `ip route show default`, зонд к 1.1.1.1 оставлен фолбэком. R5 — убраны захардкоженные Cloudflare/Google: при отсутствии резолвера отказ вместо тихой отправки DNS третьей стороне, а при восстановлении «оригинал неизвестен» файл удаляется, а не закрепляет публичные резолверы навсегда. R6 — `exclude` удаляет только маршрут, созданный нами. R7 — маркер resolvectl стал per-interface. R8 — единый залоченный create-or-read вместо второго незалоченного пути. **403 теста**, `fmt` и `clippy -D warnings` чисты. |
| 2026-07-28 | Партия 7 (сервер): **S1, S2, S4, S5**. S1 оказался ровно тем, что предсказывал собственный док-комментарий реестра («слот стал бы `Arc<Mutex<T>>` под кратким локом»): посылка «один туннель, один поток» никогда не была верна для реальных клиентов — C# гоняет upload/download/heartbeat разными задачами и создаёт отдельный realtls-handle на каждый bonded-стрим, Android допускает до 8. Добавлен тест, который зависает по таймауту, если лок снова начнут держать на всю операцию. **403 теста**, `fmt` и `clippy -D warnings` чисты. |
| 2026-07-28 | Партия 6: **E4** (QUIC-маскировка шлёт Initial вместо Handshake — поле Token Length стало легальным; проверено, что `looks_like_quic_initial` смотрит только на long-header бит и версию, поэтому совместимость в обе стороны сохраняется), **E7**, **X1**, **X2**, **P1**, **P3**, **P4**. **402 теста**, `fmt` и `clippy -D warnings` чисты. |
| 2026-07-28 | **E5 отозвана — вторая моя неверная находка.** Попытка убрать пустое padding-расширение уронила `ja4_matches_chrome`: тест фиксирует опубликованный отпечаток Chrome `t13d1516h2_8daaf6152771`, и шестнадцатое расширение в нём — именно этот padding. То есть Chrome, под который матчились, его шлёт. Изменение откачено, в коде оставлен развёрнутый комментарий, чтобы это не «чинили» повторно. Вопрос порядка относительно GREASE остаётся открытым: JA4 сортирует расширения и различить не может, нужны байтовые векторы из реального захвата (идея I1). |
| 2026-07-27 | Агент по C#-клиентам: **B2, B3, B7, N1–N6 закрыты, C6 и F8 — в части C#**. Сборка всех трёх проектов Release: 0 ошибок, 0 предупреждений; оба selftest — 134 PASS / 0 FAIL с `QELI_CONFORMANCE_REQUIRED=1`. Новые находки → Z2, Z3. Не проверено в рантайме: N3 (нужен реальный Mac; при отсутствии обеих точек привязки якоря `Engage` теперь бросает — fail-closed), B2/B3/N4/N6/B7. |
